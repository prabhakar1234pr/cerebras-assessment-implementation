# Cerebras Assessment — Implementation

**Live URL (Task 1):** _(add after Vercel deploy)_

Private submission repo for the [Cerebras AI Engineer challenge](https://github.com/danielkim-cerebras/ai-model-quality-challenge).

## Contents

| Directory | Description |
|-----------|-------------|
| [`task-1-implementation/`](./task-1-implementation/) | Task 1 — Perf Projection Explorer (Next.js) |

Task 2 (evalscope) will be added in a separate directory when complete.

## Task 1 — Run locally

```bash
cd task-1-implementation
npm install
npm run dev
```

Open http://localhost:3000 — Models A–K are pre-loaded.

## Task 1 — Deploy on Vercel

1. Import this repository in [Vercel](https://vercel.com).
2. Set **Root Directory** to `task-1-implementation`.
3. Deploy (framework: Next.js, default build settings).

## Reviewer access

Grant `sophies-cerebras`, `danielkim-cerebras`, and `kevint-cerebras` access to this private repo per the challenge instructions.
